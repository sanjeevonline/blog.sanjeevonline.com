#social_bottom.topsocial
  .social
    - var socialstags = _.chain(article.metadata.tags).map(function(tag) {
    -   return "#" + tag
    - }).value().join(" ")
      a(href=article.url, class="twitter-share-button", data-url=locals.url + article.url, data-text=article.title + " #{socialstags} /via @sandee_sh", data-count="vertical"): twitter-share-button
    | <script type="text/javascript" src="//platform.twitter.com/widgets.js"></script>
  .social
    <g:plusone size="tall" href="#{locals.url}#{article.url}"></g:plusone>
  .social
    | <script src="http://platform.linkedin.com/in.js" type="text/javascript"></script>
    | <script type="IN/Share" data-url="#{locals.url}#{article.url}" data-counter="top"></script>
  .social
    .share-delicious
      #delic.delicious 0
      #delicious-b
        a(href='http://del.icio.us/post?url=#{locals.url}#{article.url}&title="Save To Delicious"', target='blank') delicious
    |  <script type='text/javascript'>
    |  function delic(info) {
    |   if(!info[0]) return;
    |   var num = info[0].total_posts;
    |    if(!num) return;
    |    var delic = document.getElementById('delic');
    |    delic.innerHTML = '' + num;
    |    delic.style.textIndent = 0;
    |    delic.style.paddingRight= '6px';
    |  }
    |  </script>
    |  <script src='http://badges.del.icio.us/feeds/json/url/data?url=#{locals.url}#{article.url}'&callback=declic></script>
| <script type="text/javascript">
|   (function() {
|     var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true;
|     po.src = 'https://apis.google.com/js/plusone.js';
|     var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(po, s);
|   })();
| </script>

hr
footer.footer
      div.inner(style="font-size: 13px")
        block footer
          p(class="pull-right"): a(href="#") back to top
          p
            | Posted on #{article.date}
            | | Tags :&nbsp;
            each tag in article.metadata.tags
                         a(href=locals.baseUrl + "/tag/#{tag}.html", rel="category tag", title="View all posts in '#{tag}'")
                           = tag
                         &nbsp;